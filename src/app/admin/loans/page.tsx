import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AdminLoansPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Loans</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Loan management interface goes here.</p>
      </CardContent>
    </Card>
  );
}
